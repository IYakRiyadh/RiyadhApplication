import 'package:flutter/material.dart';

class MenuKopuListA extends StatelessWidget {
  const MenuKopuListA({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          'data 3',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class MenuKopuList extends StatelessWidget {
  const MenuKopuList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          'data',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}

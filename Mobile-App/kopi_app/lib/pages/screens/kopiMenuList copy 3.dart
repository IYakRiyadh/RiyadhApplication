import 'package:flutter/material.dart';

class MenuKopuListB extends StatelessWidget {
  const MenuKopuListB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          'data 2',
          style: TextStyle(fontSize: 50),
        ),
      ),
    );
  }
}
